{
  "name": "spacetimedb-combat",
  "version": "1.0.0",
  "description": "8-bit MMO combat module",
  "scripts": {
    "build": "spacetime build",
    "publish": "spacetime publish 8bit-mmo",
    "publish-delete": "spacetime publish 8bit-mmo --delete-data",
    "publish-maincloud": "spacetime publish 8bit-mmo --server maincloud -y --delete-data",
    "generate": "spacetime generate --lang typescript --out-dir ../reallife-mmo/src/generated --module-path ."
  },
  "dependencies": {
    "spacetimedb": "^2.0.2"
  },
  "devDependencies": {
    "typescript": "~5.6.2"
  }
}

{
  "name": "spacetimedb-combat",
  "version": "1.0.0",
  "description": "8-bit MMO combat module",
  "scripts": {
    "build": "spacetime build",
    "publish": "spacetime publish 8bit-combat",
    "publish-maincloud": "spacetime publish 8bit-combat --server maincloud -y",
    "generate": "spacetime generate --lang typescript --out-dir ../reallife-mmo/src/generated --module-path ."
  },
  "dependencies": {
    "spacetimedb": "^2.0.2"
  },
  "devDependencies": {
    "typescript": "~5.6.2"
  }
}
